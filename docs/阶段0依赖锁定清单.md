# 阶段 0 依赖锁定清单

日期：2026-09-18（G09完整候选审计；本次用户确认冻结；版本未主动升级）  
状态：工具链、完整依赖lock、Android API24与iOS15已冻结；最终三方通知、许可证义务检查及法律/发布复核保留为发布门禁。正式状态以 `阶段状态与决策记录.md` 为准。  
原型目录：`stage0/prototypes`  
Android 行为基线：`/Users/sn/Desktop/workplace/WanAndroid-AI` @ `78cdaade84ef24ebbe825041b499cbe1cd7ee286`

## 1. 工具链基线

| 工具 | 锁定版本/状态 | 证据 |
|---|---|---|
| Flutter | 3.47.4 stable，revision `9584c6713b` | `flutter --version` |
| Dart | 3.13.3 | `flutter --version` |
| DevTools | 2.60.0 | `flutter --version` |
| Android SDK | 36.1.0；platform 36；build-tools 36.1.0 | `flutter doctor -v` |
| Android Emulator | 36.3.10 | `flutter doctor -v` |
| Java | JetBrains Runtime 21.0.8 | `flutter doctor -v` |
| Xcode | 27.0，build 27A266a | `flutter doctor -v` |
| CocoaPods | 1.16.2 | `flutter doctor -v` |

2026-09-17本机 `flutter doctor -v` 的 Android、Xcode、CocoaPods 检查通过。用户确认 SDK 来自官方渠道并授权后，已移除实际执行文件与缓存中的 `com.apple.quarantine`；保留不造成执行阻塞的 `com.apple.provenance`。修复后原型 `flutter analyze` 通过，7 个受控单元测试通过。此结果只证明本机工具链与受控测试，不替代 Android/iOS 设备原型。

## 2. 平台最低版本

| 平台 | 冻结值 | 产品冻结状态与依据 |
|---|---:|---|
| Android | API 24 | 完整依赖debug构建通过；2026-09-18用户接受报告15.3推荐值 |
| iOS | 15.0 | 完整依赖iOS Simulator debug构建通过；2026-09-18用户接受报告15.3推荐值 |

原方案中的iOS13保留为历史候选，不再是当前目标。若要下调冻结的iOS15，需先变更冻结契约并完成插件、编译和运行专项验证。

## 3. 生产依赖

以下完整约束已由 Flutter 3.47.4 / Dart 3.13.3 成功解析并由用户冻结；精确版本以 `stage0/dependency_lock/pubspec.lock` 为准。`stage0/prototypes/pubspec.lock` 只锁定当前WebView与导航最小原型依赖，不能作为正式依赖全集。

| 能力 | Package | 直接约束 | 锁定版本 | 使用边界 |
|---|---|---:|---:|---|
| 状态与 DI | `flutter_riverpod` | `^3.4.3` | 3.4.3 | ViewModel、依赖装配；根作用域关闭隐式业务重试 |
| Riverpod 标注 | `riverpod_annotation` | `^4.0.7` | 4.0.7 | 仅配合生成器，不在 Provider 初始化中执行写操作 |
| HTTP | `dio` | `^5.11.1` | 5.11.1 | 只在 Network Service/DataSource；上层不暴露 Dio 类型 |
| JSON 标注 | `json_annotation` | `^4.12.0` | 4.12.0 | 网络 DTO 明确解析 |
| 不可变模型标注 | `freezed_annotation` | `^3.1.0` | 3.1.0 | 复杂 UiState/Model 按需使用 |
| 路由 | `go_router` | `^18.0.1` | 18.0.1 | 类型化路由、Shell、恢复；Feature 声明、App 聚合 |
| 普通偏好 | `shared_preferences` | `^2.5.5` | 2.5.5 | 使用异步 API；不保存凭据或大量数据 |
| 安全存储 | `flutter_secure_storage` | `^11.2.0` | 11.2.0 | 只由 SessionStorage/提交协调器访问 |
| 数据库 | `drift` | `^2.35.0` | 2.35.0 | 阅读历史和后续离线正文 |
| Flutter 数据库入口 | `drift_flutter` | `^0.3.1` | 0.3.1 | 平台数据库打开与路径适配 |
| WebView | `webview_flutter` | `^4.14.1` | 4.14.1 | 阅读页；不共享 API Cookie，不建立 JS Bridge |
| 外部跳转 | `url_launcher` | `^6.3.2` | 6.3.2 | 经 URL Policy 和用户确认后调用 |
| 路径工具 | `path` | `^1.9.1` | 1.9.1 | 文件/数据库路径组合，不承载业务规则 |

`cupertino_icons`约束为`^1.0.8`、完整lock解析为1.0.9；它是`flutter create`的脚手架依赖，不属于已确认生产能力。阶段1为保持用户已冻结的完整lock字节一致而保留直接约束，但正式UI不以它替换固定Android图标；以后移除会改变冻结lock，须按状态记录的契约变更流程处理。

## 4. 开发依赖

| 能力 | Package | 直接约束 | 锁定版本 |
|---|---|---:|---:|
| Lint | `flutter_lints` | `^6.0.0` | 6.0.0 |
| 生成调度 | `build_runner` | `^2.16.1` | 2.16.1 |
| Riverpod 生成 | `riverpod_generator` | `^4.0.9` | 4.0.9 |
| JSON 生成 | `json_serializable` | `^6.14.1` | 6.14.1 |
| Freezed 生成 | `freezed` | `^4.0.1` | 4.0.1 |
| Drift 生成 | `drift_dev` | `^2.35.0` | 2.35.0 |
| 类型安全路由生成 | `go_router_builder` | `^4.5.0` | 4.5.0 |
| 设备集成测试 | `integration_test` | Flutter SDK | 当前 Flutter SDK 内置版本 |

## 5. 明确不引入

| Package/类别 | 当前决定 | 理由 |
|---|---|---|
| `dio_cookie_manager` | 不引入 | 自动保存响应 Cookie 会绕过登录暂存与会话代次提交规则 |
| `permission_handler` | 不引入 | 第一版没有真实运行时权限调用方 |
| 通用分页 UI 插件 | 不引入 | 无法完整表达 0/1 起始页、分阶段错误、旧结果隔离和连续无新增暂停 |
| 全局事件总线 | 不引入 | 会话与收藏由 Repository 单一事实源发布 |
| `sqlite3_flutter_libs` / `sqlcipher_flutter_libs` 直接依赖 | 不引入 | `drift_flutter 0.3.1` 仅以 `+eol` 占位依赖阻止旧版本，实际 SQLite 构建使用 `sqlite3` hooks |

## 6. 风险与冻结条件

- `webview_flutter` 的回调没有加载请求 ID；阶段0已验证“重建Controller/Delegate并以实例token隔离”可拒绝双端同URL旧实例迟到回调，同Controller同URL请求仍不能可靠区分。
- `flutter_secure_storage.write` 是异步操作；必须通过串行提交原型验证退出、切号和后继失败。
- iOS 15已冻结；原方案iOS13仅保留为历史候选，下调需变更冻结契约并专项验证。
- 单元测试、Android/iOS实际WebView、`flutter analyze`、完整依赖双端debug构建和许可证文件盘点已完成；用户已接受将最终三方通知、许可证义务检查及法律/发布复核留到发布流程。
- 不在阶段 0 顺便升级已解析版本；如某版本阻断双端原型，记录证据后只调整必要 Package。

## 7. 验证缺口关联（2026-09-18）

- [行为对照矩阵](阶段0行为对照矩阵.md) P0-11关联本清单；[关键原型报告第13节](阶段0关键原型报告.md#13-g09-完整候选依赖与工具链审计2026-09-18)记录许可证文件、完整依赖兼容、最低版本与双端构建结果。技术版本已冻结；法务与发布复核仍待发布门禁执行，iOS13专项不再适用。
- `go_router ^18.0.1`阶段0原型证据及非活动分支失败保持历史；阶段1正式工程已用该冻结版本和`go_router_builder`生成类型安全Shell路由，并实现可恢复分支栈来源。动态Integration仍受环境阻塞，不等于完整恢复已验收，也不等于Cookie生产集成完成。
- UI资源转换还需记录来源/许可；候选截图及元数据在同一行为矩阵登记，不把忽略目录素材作为CI唯一依赖。
- 阶段1根`pubspec.lock`与`stage0/dependency_lock/pubspec.lock`的SHA-256均为`a4cc43f56833a8b00bffff13bbe618b29c7f8603787fcb444948aa1c0726efb3`；这只证明lock身份一致，不替代正式工程双端构建。

## 8. G01/G02/G04复验关联（2026-09-18）

重新读取Flutter3.47.4/Dart3.13.3；该轮受控22/22、Analyze、format通过。Android15/iOS26.2同URL竞态、历史、滚动、reload及重建各1/1通过，本次Android未复现历史Maven下载阻塞。该段是G08/G09之前的历史记录；后续完整依赖双端构建和许可证文件盘点见第10节，最低版本与法务/发布取舍仍待确认。详细执行与限制见[原型报告第8节](阶段0关键原型报告.md#8-g02g04-双端同-url-竞态与刷新差异2026-09-18)和状态入口。

## 9. G08导航原型依赖关联（2026-09-18）

最小原型新增直接依赖`go_router ^18.0.1`并解析为18.0.1，`pubspec.lock`随之加入其传递依赖；双端导航专项与全量集成已通过，具体能力和失败边界见[原型报告第12节](阶段0关键原型报告.md#12-g08-导航守卫与恢复边界2026-09-18)。最小原型没有接入`go_router_builder`，因为阶段0没有生成式路由调用方；它只在完整依赖生成器图中完成加载验证。该版本现已随完整lock冻结。

## 10. G09完整候选审计结果（2026-09-18）

| 项目 | 实际结果 | 边界 |
|---|---|---|
| 完整解析 | 离线成功；151节点；完整lock SHA-256 `a4cc43f56833a8b00bffff13bbe618b29c7f8603787fcb444948aa1c0726efb3` | 未主动升级；完整lock仍不是最小原型lock |
| 许可证文件 | 142个hosted包全部有LICENSE/COPYING；BSD-style 115、MIT 20、Apache-2.0 7；无NOTICE或未分类文本 | 自动文本盘点，不替代法务与最终三方清单 |
| 生成器 | 完整builder图25秒加载成功、0输出 | 无注解源码；模板级生成留阶段1真实调用方 |
| Android完整依赖 | debug APK 75.8秒通过；min24/target36 | sqlite3构建要求CMake3.22.1；非release/真机 |
| iOS完整依赖 | Simulator debug 44.6秒通过；MinimumOSVersion15.0 | 主要podspec虽声明13，完整应用13未验证；非签名/真机 |
| 恢复最小原型 | pubspec/lock哈希恢复；插件注册无临时插件；format、Analyze、39/39通过 | 双端integration沿用G08最终全量证据，源码未变 |

技术证据详见[原型报告第13节](阶段0关键原型报告.md#13-g09-完整候选依赖与工具链审计2026-09-18)。用户已冻结SDK/依赖起始版本、Android API24与iOS15；debug/Simulator证据仍不替代Release、签名、真机和发布复核。
